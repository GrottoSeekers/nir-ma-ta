<section id="top" class="hero">
  <div class="wrap hero-grid">
    <div class="hero-copy">
      <span class="badge">SENGGIGI &middot; LOMBOK</span>
      <h1>Lombok's soul,<br /><span class="red">brewed to perfection.</span></h1>
      <p class="hero-sub">Sejak hari ini — since day one.</p>
      <p class="hero-lead">A one-man cart on the road through Senggigi, now a shop of its own.</p>
      <div class="hero-cta-row">
        <a href="https://www.google.com/maps/search/?api=1&query=NIR-MA-TA+COFFEE+Senggigi+Lombok" target="_blank" rel="noopener noreferrer" class="btn btn-ink">
          GET DIRECTIONS
        </a>
        <a href="#menu" class="btn btn-outline-ink">SEE THE MENU</a>
      </div>
    </div>
    <div class="hero-visual">
      <img
        src="/images/cart-street.webp"
        alt="Juandi serving coffee from the NIR-MA-TA cart on the roadside in Senggigi"
        width="1000"
        height="1777"
      />
    </div>
  </div>
</section>

<style>
  .hero {
    padding-top: clamp(36px, 6vw, 64px);
  }

  .hero-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(100%, 320px), 1fr));
    gap: 48px;
    align-items: center;
  }

  .badge {
    display: inline-block;
    background: var(--red);
    color: #fff;
    padding: 6px 12px;
    font-family: "Work Sans", sans-serif;
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 1.6px;
    margin-bottom: 24px;
  }

  h1 {
    font-size: clamp(42px, 6.6vw, 80px);
    line-height: 0.95;
    letter-spacing: -2.5px;
    margin: 0 0 10px;
  }

  h1 .red {
    color: var(--red);
  }

  .hero-sub {
    font-family: "Archivo", sans-serif;
    font-weight: 600;
    font-size: 20px;
    letter-spacing: 0.5px;
    color: var(--red);
    margin: 0 0 22px;
  }

  .hero-lead {
    font-size: 18px;
    line-height: 1.6;
    color: var(--body-grey);
    margin: 0 0 32px;
    max-width: 480px;
    text-wrap: pretty;
  }

  .hero-cta-row {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }

  .hero-visual img {
    width: 100%;
    height: clamp(360px, 56vh, 560px);
    object-fit: cover;
    object-position: center 72%;
  }
</style>
